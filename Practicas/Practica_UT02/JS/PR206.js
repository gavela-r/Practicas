let numero = prompt("indicame un numero");
let valor = prompt("indicame un valor");

for (let k = 0; k <= valor; k++){
    let resultado = numero * valor;
    console.log(`${numero} * ${valor} = ${resultado} `);
}
